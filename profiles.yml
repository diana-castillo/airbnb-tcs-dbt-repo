deploy_dbt_airbnb:
  target: dev
  outputs:
    dev:
      type: snowflake
      account: rm66342.us-east-2.aws

      # User/password auth
      user: dbt_user1
      password: doe

      role: DBT_DEVELOPER
      database: AIRBNB
      warehouse: DBT_WAREHOUSE
      # in the future you should switch this schema to use a new environment variable
      # definitely switch this to your schema though
      schema: DEV
      threads: 4
      client_session_keep_alive: False
    prod:
      type: snowflake
      account: rm66342.us-east-2.aws

      # User/password auth
      user: dbt_user1
      password: doe

      role: DBT_DEVELOPER
      database: AIRBNB
      warehouse: DBT_WAREHOUSE
      schema: PRODUCTION
      threads: 4
      client_session_keep_alive: False
